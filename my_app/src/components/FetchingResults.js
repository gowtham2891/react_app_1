import React from 'react';
import './FetchingResults.module.css';

const FetchingResults = () => {
  return (
    <div className="fetching-results">Fetching the results...</div>
  );
};

export default FetchingResults;
