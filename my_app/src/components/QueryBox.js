import React from 'react';
import './QueryBox.module.css';

const QueryBox = () => {
  return (
    <input type="text" className="query-box" placeholder="Enter query" />
  );
};

export default QueryBox;
